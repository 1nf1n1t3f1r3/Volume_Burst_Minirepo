Backup/Storage Minirepo for Volume Burst Python/Pinescript. Not intended as stand-alone financial advice.

The Python script will require it's own environment and dependencies to run (Apologies). Pinescript can be added to any chart. Copy/Paste the code into a new script, or simply search for 'Volume Burst', you should find my script by 'Comatus'.
